node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example
secrets/
*.token
*.key
*.pem
.DS_Store
bun.lockb.bak
coverage/
.bun/
# python test env / caches
.venv/
.venv-test/
__pycache__/
*.pyc
# runtime state
channel.env
config.json
bot.pid
inbox/
state/
# state/ above targets the RUNTIME state dir, but it also matches the source
# folder plugin/src/state/ — which silently dropped src/state/store.ts from git
# and broke the channel server at boot. Keep the source tree tracked.
!plugin/src/state/
!plugin/src/state/**
