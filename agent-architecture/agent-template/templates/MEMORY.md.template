# MEMORY -- {{AGENT_NAME}} (archive)

_Permanent archive. NOT loaded into session context. Access via Read tool when needed._
_Curated long-term notes. Aged-out memory lands under `core/archived/` automatically:
size-rolled episodic slices via `archive-roll.sh`, decayed passive insights via
`decay-sweep.sh` (pure-bash nightly housekeeping, no model)._
