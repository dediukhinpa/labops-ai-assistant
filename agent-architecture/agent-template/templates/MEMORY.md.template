# MEMORY -- {{AGENT_NAME}} (ARCHIVE archive)

_Permanent archive. NOT loaded into session context. Access via Read tool when needed._
_Entries rotated here from PASSIVE (>14 days) by rotate-passive.sh._
