# AGENTS.md -- {{AGENT_NAME}} Configuration

## Models

- **Primary:** {{PRIMARY_MODEL}} (e.g. Claude Opus 4.8)
- **Subagents:** Native Claude Code Agent tool (inherit parent model or override in agents/*.md)
- **Research:** {{RESEARCH_MODEL}} (e.g. Perplexity Sonar -- web search only, no code)

**Rules:**
- Subagents: ONLY native Claude Code Agent tool
- Maximum {{MAX_SUBAGENTS}} subagents simultaneously
- Heavy work -> subagents. Architecture and review -> main agent.

## Message Bus

| Operation | How |
|-----------|-----|
| My inbox | `{{INBOX_READ_CMD}}` |
| Send message | `{{MSG_SEND_CMD}}` |
| Update status | `{{STATUS_UPDATE_CMD}}` |

## Task Acceptance

1. Task unclear? -> ask clarifying questions
2. Task not mine? -> reject with reasoning, suggest who
3. Task clear? -> execute or delegate to subagent

Do NOT assume. Do NOT redirect without operator.

## Pipelines

| Pipeline | Steps |
|----------|-------|
| dev | plan -> code -> test -> review -> deploy |
| code | implement -> test -> commit |
| fix | reproduce -> fix -> test -> commit |
| review | read -> analyze -> comment -> approve/reject |
| research | search -> analyze -> propose |

## Cross-Review

- Cross-review on different models always
- Author != reviewer

## second_brain (L4 Shared Semantic Memory)

- MCP host: `${MCP_HOST}` — host/IP only, no protocol or port (e.g. `127.0.0.1` or `mcp.example.com`)
- Bearer token: stored in `.mcp.json` (see `agent-template/templates/mcp.json.template`)
- Recall: MCP tool `recall` on `${SECOND_BRAIN_MEMORY_ROUTER_URL}` (default `http://${MCP_HOST}:5002/mcp`) — JSON-RPC `tools/call`, args `{"query":"<topic>","limit":10}`
- Write: MCP tools `create_decision_note`, `create_error_pattern_note`, `create_external_note` on `${SECOND_BRAIN_MEMORY_URL}` (default `http://${MCP_HOST}:5001/mcp`)
- Swarm: MCP tool `notify` on `${SECOND_BRAIN_AGENT_ROUTER_URL}` (default `http://${MCP_HOST}:5000/mcp`) for inter-agent triggers

## Team

| Agent | Role | Channel |
|-------|------|---------|
| {{AGENT_1_NAME}} | {{AGENT_1_ROLE}} | {{AGENT_1_CHANNEL}} |
| {{AGENT_2_NAME}} | {{AGENT_2_ROLE}} | {{AGENT_2_CHANNEL}} |
| {{AGENT_3_NAME}} | {{AGENT_3_ROLE}} | {{AGENT_3_CHANNEL}} |

## Self-Learning

Local-only learning system. See [LEARNINGS.md](../LEARNINGS.md) for full documentation.

### Access Zones

| Zone | Files | Who changes |
|------|-------|------------|
| **RED (read-only)** | CLAUDE.md, rules.md | Operator only |
| **YELLOW (self-edit)** | USER.md, AGENTS.md, TOOLS.md, warm/, hot/ | Agent on trigger |
| **GREEN (autonomy)** | LEARNINGS.md, MEMORY.md, skills/*, feedback_*.md | Agent freely |

### Flow

1. Event -> classify type (tool / behavior / workflow / architecture)
2. Record in core/LEARNINGS.md (structured log)
3. Record in auto-memory feedback_*.md if behavioral
4. If stable pattern -> update YELLOW zone

### Metric

Repeats column in LEARNINGS.md -- recurrence counter. >0 = rule not working, strengthen.
