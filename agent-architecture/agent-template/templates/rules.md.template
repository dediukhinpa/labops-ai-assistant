# RULES -- {{AGENT_NAME}}

_Rules I earned from my own mistakes. Empty at install. A rule lands here when the operator
says "write yourself a rule", or when the same correction comes a second time -- then I
propose the rule and add it once the operator agrees. Zones and way of working live in
CLAUDE.md; rules shared by every agent -- in ~/.claude/CLAUDE.md. Always in context._

_Format: `- <rule> -- <why, in one line> (YYYY-MM-DD)`_

## Rules
