{
  "mcpServers": {
    "second_brain-memory": {
      "type": "http",
      "url": "${SECOND_BRAIN_MEMORY_URL}",
      "headers": { "Authorization": "Bearer ${AGENT_BEARER}" }
    },
    "second_brain-memory_router": {
      "type": "http",
      "url": "${SECOND_BRAIN_MEMORY_ROUTER_URL}",
      "headers": { "Authorization": "Bearer ${AGENT_BEARER}" }
    },
    "second_brain-agent_router": {
      "type": "http",
      "url": "${SECOND_BRAIN_AGENT_ROUTER_URL}",
      "headers": { "Authorization": "Bearer ${AGENT_BEARER}" }
    }
  }
}
